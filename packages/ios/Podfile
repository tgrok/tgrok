# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'Tgrok' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'SwiftyJSON', '~> 4.0'

  # Pods for Tgrok

  target 'TgrokTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TgrokUITests' do
    # Pods for testing
  end

end
